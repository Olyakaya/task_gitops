apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

configMapGenerator:
  - name: myapp-config
    literals:
      - GREETING_MESSAGE="Hello, Azure! deploy by Argo"
